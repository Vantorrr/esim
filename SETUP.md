# 🚀 Инструкция по настройке eWave

## Шаг 1: Регистрация на esim-go.com

1. Перейдите на https://esim-go.com
2. Нажмите "Sign Up" для регистрации
3. Заполните форму регистрации:
   - Email
   - Пароль
   - Название компании
4. Подтвердите email
5. Войдите в аккаунт

### Получение API ключа

1. В панели управления перейдите в раздел **API**
2. Нажмите **"Create API Key"**
3. Дайте имя ключу (например, "eWave Production")
4. Скопируйте API ключ (он показывается только один раз!)
5. Добавьте в файл `.env`:
   \`\`\`
   ESIM_GO_API_KEY=your_api_key_here
   \`\`\`

### Тестирование API

Документация: https://docs.esim-go.com

Проверьте подключение:
\`\`\`bash
curl -X GET "https://api.esim-go.com/v2.2/countries" \\
  -H "X-API-Key: your_api_key_here"
\`\`\`

## Шаг 2: Настройка Stripe

### Регистрация

1. Перейдите на https://stripe.com
2. Нажмите "Start now"
3. Заполните форму регистрации
4. Подтвердите email

### Получение API ключей

1. В дашборде перейдите в **Developers → API keys**
2. Скопируйте:
   - **Publishable key** (начинается с pk_)
   - **Secret key** (начинается с sk_)
3. Добавьте в `.env`:
   \`\`\`
   STRIPE_SECRET_KEY=sk_test_...
   STRIPE_PUBLISHABLE_KEY=pk_test_...
   \`\`\`

### Настройка Webhooks

1. В Stripe перейдите в **Developers → Webhooks**
2. Нажмите **"Add endpoint"**
3. URL endpoint: `https://your-domain.com/api/webhook/stripe`
4. Выберите события:
   - `checkout.session.completed`
   - `payment_intent.succeeded`
   - `payment_intent.payment_failed`
5. Скопируйте **Signing secret** (начинается с whsec_)
6. Добавьте в `.env`:
   \`\`\`
   STRIPE_WEBHOOK_SECRET=whsec_...
   \`\`\`

### Тестирование webhook локально

Установите Stripe CLI:
\`\`\`bash
brew install stripe/stripe-cli/stripe

# Авторизуйтесь
stripe login

# Перенаправьте webhook на локальный сервер
stripe listen --forward-to localhost:8080/api/webhook/stripe
\`\`\`

## Шаг 3: Настройка ЮKassa (опционально)

### Регистрация

1. Перейдите на https://yookassa.ru
2. Нажмите "Подключить ЮKassa"
3. Заполните форму (нужны данные ИП/ООО)
4. Дождитесь модерации (1-3 дня)

### Получение ключей

1. В личном кабинете перейдите в **Настройки → Параметры API**
2. Скопируйте:
   - **shopId**
   - **Секретный ключ**
3. Добавьте в `.env`:
   \`\`\`
   YOOKASSA_SHOP_ID=123456
   YOOKASSA_SECRET_KEY=live_...
   \`\`\`

### Настройка уведомлений

1. В настройках перейдите в **HTTP-уведомления**
2. URL: `https://your-domain.com/api/webhook/yookassa`
3. Включите уведомления для:
   - payment.succeeded
   - payment.canceled

## Шаг 4: Создание Telegram бота

### Создание бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например, "eWave eSIM Bot")
4. Введите username бота (например, "ewave_esim_bot")
5. Скопируйте токен и добавьте в `.env`:
   \`\`\`
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
   \`\`\`

### Настройка Mini App

1. Отправьте команду `/newapp` в BotFather
2. Выберите вашего бота
3. Введите название приложения: **eWave eSIM**
4. Введите описание:
   \`\`\`
   Купить eSIM для путешествий. Интернет в 200+ странах. 
   Мгновенная активация через QR-код. 🌍✈️
   \`\`\`

### Загрузка изображений

5. Команда `/setappphoto` - загрузите квадратную иконку 640x640px
6. Команда `/setdescriptionpicture` - загрузите превью 1280x640px

### Установка URL Mini App

7. Команда `/setappdomain`
8. Введите домен: `your-domain.com` (без https://)

9. Команда `/setappurl`
10. Введите полный URL: `https://your-domain.com`

### Настройка меню

11. Команда `/setmenubutton`
12. Выберите вашего бота
13. Введите текст кнопки: **🌊 Открыть eWave**
14. Введите URL: `https://your-domain.com`

## Шаг 5: Развертывание приложения

### Вариант 1: Vercel (Frontend) + Railway (Backend)

#### Frontend на Vercel

\`\`\`bash
# Установите Vercel CLI
npm install -g vercel

# Деплой
vercel

# Следуйте инструкциям в терминале
\`\`\`

Добавьте переменные окружения в Vercel:
- Settings → Environment Variables
- Добавьте `NEXT_PUBLIC_API_URL`

#### Backend на Railway

1. Перейдите на https://railway.app
2. Создайте новый проект "New Project"
3. Выберите "Deploy from GitHub repo"
4. Подключите ваш репозиторий
5. В настройках добавьте переменные окружения из `.env`
6. Укажите:
   - **Start Command**: `node server/index.js`
   - **Port**: 8080

### Вариант 2: VPS (Ubuntu)

\`\`\`bash
# Подключитесь к серверу
ssh user@your-server-ip

# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите PM2
sudo npm install -g pm2

# Клонируйте репозиторий
git clone https://github.com/your-repo/ewave.git
cd ewave

# Установите зависимости
npm install

# Создайте .env файл
nano .env
# Вставьте все переменные окружения

# Соберите frontend
npm run build

# Запустите backend через PM2
pm2 start server/index.js --name ewave-backend

# Запустите frontend через PM2
pm2 start npm --name ewave-frontend -- start

# Сохраните конфигурацию PM2
pm2 save
pm2 startup

# Настройте Nginx
sudo apt install nginx
sudo nano /etc/nginx/sites-available/ewave
\`\`\`

Конфигурация Nginx:
\`\`\`nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
\`\`\`

\`\`\`bash
# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/ewave /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx

# Установите SSL через Certbot
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
\`\`\`

## Шаг 6: Тестирование

### Локальное тестирование

1. Запустите оба сервера:
   \`\`\`bash
   npm run server:dev  # Backend
   npm run dev         # Frontend
   \`\`\`

2. Используйте ngrok для тестирования в Telegram:
   \`\`\`bash
   ngrok http 3000
   \`\`\`

3. Укажите ngrok URL в BotFather через `/setappurl`

### Проверьте функциональность:

- ✅ Загрузка списка стран
- ✅ Выбор страны и отображение пакетов
- ✅ Переход на страницу оплаты
- ✅ Создание платежной сессии
- ✅ Webhook после оплаты
- ✅ Получение QR-кода eSIM
- ✅ Отображение QR-кода и инструкций

## 🆘 Troubleshooting

### API esim-go не работает

- Проверьте API ключ
- Проверьте баланс аккаунта
- Смотрите логи: `tail -f server/logs/api.log`

### Stripe webhook не получает события

- Проверьте URL webhook
- Проверьте signing secret
- Используйте Stripe CLI для локального тестирования

### Telegram Mini App не открывается

- Проверьте HTTPS (обязательно!)
- Проверьте домен в BotFather
- Проверьте CORS настройки
- Откройте в браузере напрямую для отладки

### База данных нужна?

Для MVP база не обязательна, но для production рекомендуется:
- PostgreSQL для хранения заказов
- Redis для кэширования тарифов
- MongoDB для логов и аналитики

## 📞 Поддержка

Если возникли проблемы:
- Проверьте логи: `pm2 logs`
- Смотрите документацию: README.md
- Напишите в issues на GitHub

---

Удачи с запуском! 🚀

