#!/bin/bash

echo "🌊 eWave - Тест eSIM-GO API"
echo "================================"
echo ""

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

# Проверка что сервер запущен
echo "📡 Проверяю сервер..."
if curl -s http://localhost:8080/api/health > /dev/null 2>&1; then
    echo -e "${GREEN}✅ Сервер работает!${NC}"
else
    echo -e "${RED}❌ Сервер не запущен!${NC}"
    echo "Запусти сервер командой: npm run server:dev"
    exit 1
fi

echo ""
echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Тест 1: Получить список стран
echo "${YELLOW}🌍 Тест 1: Получаю список стран...${NC}"
COUNTRIES=$(curl -s http://localhost:8080/api/esim/countries)
if [ $? -eq 0 ]; then
    echo -e "${GREEN}✅ Список стран получен!${NC}"
    echo "$COUNTRIES" | jq '.' 2>/dev/null || echo "$COUNTRIES"
else
    echo -e "${RED}❌ Ошибка при получении стран${NC}"
fi

echo ""
echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

# Тест 2: Получить пакеты для США
echo "${YELLOW}📦 Тест 2: Получаю пакеты для США...${NC}"
PACKAGES=$(curl -s "http://localhost:8080/api/esim/packages?country=US")
if [ $? -eq 0 ]; then
    echo -e "${GREEN}✅ Пакеты получены!${NC}"
    echo "$PACKAGES" | jq '.' 2>/dev/null || echo "$PACKAGES"
else
    echo -e "${RED}❌ Ошибка при получении пакетов${NC}"
fi

echo ""
echo "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo ""

echo "${GREEN}🎉 Тестирование завершено!${NC}"
echo ""
echo "📝 Что дальше:"
echo "  1. Открой http://localhost:3000 в браузере"
echo "  2. Выбери страну (например, USA)"
echo "  3. Увидишь доступные тарифы eSIM!"
echo ""

