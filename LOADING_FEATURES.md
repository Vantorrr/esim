# 🎨 Загрузочные заставки eWave

## 📱 Что добавлено:

### **1. LoadingScreen** - основная загрузка
**Файл:** `components/LoadingScreen.tsx`

**Особенности:**
- ✨ Анимированные волны на фоне
- 🎯 Плавающие частицы
- 📊 Progress bar с анимацией
- 🌊 Лого с эффектом bounce
- 💫 Glassmorphism эффекты
- 🎨 Градиенты синий + оранжевый

**Используется:**
- Главная страница (`/`)
- Страница оплаты (`/checkout`)
- Страница успеха (`/success`)

### **2. SplashScreen** - первый экран
**Файл:** `components/SplashScreen.tsx`

**Особенности:**
- ⚡ Быстрая анимация (2.5 сек)
- 🌀 Круговые волны от центра
- 💫 Пульсация логотипа
- 🎭 Эффект появления с вращением

**Использование (опционально):**
Добавь в `app/layout.tsx` если хочешь splash при первом запуске

---

## 🎨 Анимации:

### **Wave Animation** (волны)
```css
- 3 слоя волн
- Плавное движение
- Прозрачность от 0.3 до 0.7
- Бесконечная анимация
```

### **Particles** (частицы)
```css
- 20 плавающих точек
- Движение снизу вверх
- Случайная задержка
- Fade in/out эффект
```

### **Logo Effects**
```css
- Bounce (мягкий отскок)
- Pulse (пульсация свечения)
- Scale (увеличение/уменьшение)
```

### **Progress Bar**
```css
- Gradient оранжевый
- Shimmer эффект (блеск)
- Плавное заполнение
- Процент в реальном времени
```

---

## 🎯 Использование:

### Автоматически включено:
```typescript
// Уже работает на всех страницах!
import LoadingScreen from '@/components/LoadingScreen';

<LoadingScreen onFinish={() => setShowLoading(false)} />
```

### Настройка времени:
```typescript
// components/LoadingScreen.tsx
// Строка 23: меняй интервал
setInterval(() => {
  // ...
}, 200); // ← скорость загрузки (мс)
```

### Добавить на свою страницу:
```typescript
'use client';
import { useState } from 'react';
import LoadingScreen from '@/components/LoadingScreen';

export default function MyPage() {
  const [loading, setLoading] = useState(true);

  if (loading) {
    return <LoadingScreen onFinish={() => setLoading(false)} />;
  }

  return <div>Твой контент</div>;
}
```

---

## 🎨 Кастомизация:

### Изменить цвета:
```tsx
// Фон
bg-gradient-primary  // синий градиент

// Progress bar
from-secondary via-yellow-400 to-secondary  // оранжевый

// Элементы
bg-white/10  // полупрозрачный белый
```

### Изменить скорость анимаций:
```css
/* components/LoadingScreen.tsx - в <style jsx> */

/* Волны */
animation: wave-animation 10s infinite;  /* 10s → твоё время */

/* Bounce логотипа */
animation: bounce-slow 3s infinite;  /* 3s → твоё время */

/* Частицы */
animation-duration: ${3 + Math.random() * 2}s;  /* 3-5 сек */
```

### Изменить количество частиц:
```tsx
// Строка 54
{[...Array(20)].map((_, i) => (  // 20 → твоё число
```

---

## 💡 Фичи:

### **Features Icons** (внизу)
Показывают преимущества:
- ⚡ Быстро
- 🌍 200+ стран
- 🔒 Безопасно

Можно добавить свои:
```tsx
<div className="flex flex-col items-center gap-2 text-white/80">
  <div className="w-12 h-12 rounded-full bg-white/10 ...">
    💰  {/* твоя иконка */}
  </div>
  <span className="text-xs">Выгодно</span>
</div>
```

---

## 🚀 Производительность:

### Оптимизации:
- ✅ CSS анимации (не JS)
- ✅ `priority` для логотипа
- ✅ Автоматическое скрытие
- ✅ Cleanup в useEffect
- ✅ Легкие SVG эффекты

### Размер:
- LoadingScreen: ~8KB
- SplashScreen: ~3KB
- Лого: ~280KB (PNG)

---

## 📱 Mobile First:

Все анимации оптимизированы для:
- ✅ iOS Safari
- ✅ Android Chrome
- ✅ Telegram WebView
- ✅ Touch devices

---

## 🎭 Эффекты:

### Glassmorphism
```css
backdrop-blur-md        /* размытие фона */
bg-white/10            /* полупрозрачность */
border border-white/20 /* светлая граница */
```

### Drop Shadow
```css
drop-shadow-2xl  /* тень для логотипа */
shadow-lg        /* тень для прогресс бара */
```

### Gradient
```css
bg-gradient-primary    /* синий градиент */
bg-gradient-secondary  /* оранжевый градиент */
```

---

## 🐛 Troubleshooting:

### Лого не показывается?
```bash
# Проверь что файл на месте:
ls -la public/logo.png

# Должен быть ~280KB
```

### Анимации тормозят?
```tsx
// Уменьши количество частиц:
{[...Array(10)].map  // было 20

// Увеличь интервал:
}, 300);  // было 200
```

### Долго грузится?
```tsx
// Ускорь прогресс:
return prev + 20;  // было 10
}, 100);  // было 200
```

---

## 🎨 Цветовая схема:

```css
Primary (синий):
- #0080FF → #0060DD

Secondary (оранжевый):  
- #FF8534 → #FF6600

Фон:
- rgba(255, 133, 52, 0.3)  /* оранжевый прозрачный */

Текст:
- white / white/90 / white/80
```

---

## 🎯 Best Practices:

1. **Используй LoadingScreen** для всех async операций
2. **Callback onFinish** для управления состоянием
3. **Progress bar** показывай реальный прогресс если возможно
4. **Минимум 2 секунды** - пользователь должен видеть бренд
5. **Максимум 5 секунд** - не утомляй пользователя

---

## 🔥 Готово!

Твоя загрузочная заставка:
- ✅ Красивая
- ✅ Анимированная
- ✅ С брендингом
- ✅ Оптимизированная
- ✅ Везде интегрирована

**Запускай и наслаждайся!** 🌊

```bash
./start.sh
# Открой http://localhost:3000
# Увидишь крутую анимацию! 🔥
```

