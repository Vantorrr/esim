# 🤝 Contributing to eWave

Спасибо, что хотите внести свой вклад в развитие eWave! Этот документ содержит рекомендации по разработке.

## 📋 Процесс разработки

### 1. Создайте issue

Перед началом работы создайте issue с описанием:
- Что вы хотите добавить/исправить
- Почему это важно
- Как это будет работать

### 2. Fork и клонирование

\`\`\`bash
# Fork репозиторий через GitHub UI

# Клонируйте ваш fork
git clone https://github.com/YOUR_USERNAME/ewave.git
cd ewave

# Добавьте upstream
git remote add upstream https://github.com/ORIGINAL_OWNER/ewave.git
\`\`\`

### 3. Создайте ветку

\`\`\`bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
\`\`\`

### 4. Разработка

\`\`\`bash
# Установите зависимости
npm install

# Запустите в dev режиме
npm run dev
npm run server:dev
\`\`\`

### 5. Коммиты

Используйте Conventional Commits:

\`\`\`bash
# Примеры:
git commit -m "feat: добавить фильтр по цене для пакетов"
git commit -m "fix: исправить отображение QR-кода на iOS"
git commit -m "docs: обновить README с инструкциями"
git commit -m "style: улучшить отступы в компоненте Header"
git commit -m "refactor: переписать API клиент с axios на fetch"
\`\`\`

Типы коммитов:
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование, не влияет на код
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

### 6. Push и Pull Request

\`\`\`bash
# Push в ваш fork
git push origin feature/your-feature-name
\`\`\`

Создайте Pull Request через GitHub UI:
- Опишите изменения
- Прикрепите скриншоты (для UI изменений)
- Укажите номер issue (если есть)

## 🎨 Code Style

### TypeScript/JavaScript

\`\`\`typescript
// ✅ Хорошо
export function getPackages(country: string): Promise<Package[]> {
  return api.get('/packages', { params: { country } });
}

// ❌ Плохо
export function getPackages(country) {
  return api.get('/packages?country=' + country);
}
\`\`\`

### React Components

\`\`\`tsx
// ✅ Хорошо - функциональный компонент с типами
interface ButtonProps {
  onClick: () => void;
  children: React.ReactNode;
  variant?: 'primary' | 'secondary';
}

export default function Button({ onClick, children, variant = 'primary' }: ButtonProps) {
  return (
    <button
      onClick={onClick}
      className={\`btn btn-\${variant}\`}
    >
      {children}
    </button>
  );
}

// ❌ Плохо - без типов, классовый компонент
export default class Button extends React.Component {
  render() {
    return <button onClick={this.props.onClick}>{this.props.children}</button>;
  }
}
\`\`\`

### CSS (Tailwind)

\`\`\`tsx
// ✅ Хорошо - использование Tailwind классов
<div className="bg-white rounded-2xl p-6 shadow-lg hover:shadow-xl transition-shadow">

// ❌ Плохо - inline стили
<div style={{ backgroundColor: 'white', borderRadius: '16px', padding: '24px' }}>
\`\`\`

## 📁 Структура файлов

### Компоненты

\`\`\`
components/
├── Button.tsx           # Простой компонент
├── PackageCard/         # Сложный компонент
│   ├── index.tsx
│   ├── PackageCard.tsx
│   └── styles.module.css (если нужно)
\`\`\`

### Страницы (Next.js App Router)

\`\`\`
app/
├── page.tsx              # Главная страница
├── checkout/
│   └── page.tsx         # /checkout
└── success/
    └── page.tsx         # /success
\`\`\`

### API Routes

\`\`\`
server/
├── routes/
│   ├── esim.js          # /api/esim/*
│   ├── payment.js       # /api/payment/*
│   └── webhook.js       # /api/webhook/*
└── services/
    └── esimGoApi.js     # Сервис для работы с API
\`\`\`

## 🧪 Тестирование

\`\`\`bash
# Запуск тестов (когда будут добавлены)
npm test

# E2E тесты
npm run test:e2e
\`\`\`

## 📝 Документация

При добавлении новой функциональности:

1. **Обновите README.md** - если это влияет на настройку
2. **Обновите SETUP.md** - если нужны новые API ключи
3. **Добавьте комментарии** - для сложной логики
4. **Обновите типы** - TypeScript типы должны быть актуальны

## 🐛 Сообщение о багах

При создании issue о баге укажите:

\`\`\`markdown
**Описание бага:**
Краткое описание проблемы

**Шаги для воспроизведения:**
1. Перейти на ...
2. Нажать на ...
3. Увидеть ошибку

**Ожидаемое поведение:**
Что должно было произойти

**Текущее поведение:**
Что происходит на самом деле

**Скриншоты:**
(если применимо)

**Окружение:**
- OS: [например, macOS 13]
- Браузер: [например, Chrome 120]
- Версия приложения: [например, 1.0.0]

**Дополнительный контекст:**
Любая другая информация о проблеме
\`\`\`

## ✨ Идеи для улучшений

Вот что можно добавить:

### Backend
- [ ] База данных для хранения заказов
- [ ] Система уведомлений через Telegram Bot API
- [ ] Админ панель для управления
- [ ] Rate limiting для API
- [ ] Логирование и мониторинг

### Frontend
- [ ] PWA поддержка
- [ ] Темная тема
- [ ] Больше языков (ES, DE, FR)
- [ ] История заказов пользователя
- [ ] Система рефералов
- [ ] Сравнение тарифов
- [ ] Калькулятор стоимости

### Features
- [ ] Автоматическое пополнение eSIM
- [ ] Подписка на уведомления о низком балансе
- [ ] Региональные пакеты (несколько стран)
- [ ] Корпоративные тарифы
- [ ] API для B2B клиентов

## 📞 Вопросы?

- 📧 Email: dev@ewave.com
- 💬 Telegram: [@ewave_dev](https://t.me/ewave_dev)
- 🐛 Issues: [GitHub Issues](https://github.com/your-repo/ewave/issues)

---

Спасибо за вклад в развитие eWave! 🌊✨

